# Copy this file to `.env` and fill in your key. `.env` is gitignored — never commit secrets.
# Only needed for a real (paid) LLM extraction run; `--dry-run` needs no key.

ANTHROPIC_API_KEY=sk-ant-your-key-here
